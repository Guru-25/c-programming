#include <stdio.h>

void factorial(int n)
{
    int fact = 1;
    for (i = 1; i <= n; i++)
        fact = fact * i;
    printf("factorial of %d is %d", n, fact);
}
void qr(int n1, n2)
{
    q = n1 / n2;
    r = n1 % n2;
    printf("quotient is %d\nremainder is %d", q, r);
}
void roots(a, b, c)
{
    int x, y;
    x = -b + sqrt(b*b)
}

int main()
{
    

    return 0;
}